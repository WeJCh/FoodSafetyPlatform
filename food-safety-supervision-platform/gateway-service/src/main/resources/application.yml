server:
  port: 8080

spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
      discovery:
        server-addr: ${NACOS_ADDR:127.0.0.1:8848}
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD:127.0.0.1:8080}
    gateway:
      discovery:
        locator:
          enabled: true
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowed-origin-patterns:
              - "http://localhost:5173"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowed-headers:
              - "*"
            allow-credentials: true
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/user/**
        - id: user-auth
          uri: lb://user-service
          predicates:
            - Path=/api/auth/**
        - id: user-account
          uri: lb://user-service
          predicates:
            - Path=/api/users/**
        - id: user-roles
          uri: lb://user-service
          predicates:
            - Path=/api/roles/**
        - id: user-admin
          uri: lb://user-service
          predicates:
            - Path=/api/admin/**
        - id: regulation-operation-service
          uri: lb://regulation-operation-service
          predicates:
            - Path=/api/regulation-operation/**
        - id: regulation-service
          uri: lb://regulation-service
          predicates:
            - Path=/api/regulation/**
        - id: regulation-files
          uri: lb://regulation-service
          predicates:
            - Path=/api/files/**
        - id: complaint-service
          uri: lb://complaint-service
          predicates:
            - Path=/api/complaints/**
        - id: query-service
          uri: lb://query-service
          predicates:
            - Path=/api/query/**
        - id: warning-service
          uri: lb://warning-service
          predicates:
            - Path=/api/warning/**

jwt:
  secret: "foodUserServiceJwtSecretKey123456"

gateway:
  auth:
    # Introspect timeout in milliseconds to avoid blocking the gateway when user-service is slow.
    introspect-timeout-ms: 1500
    # Strict by default. Enable fail-open only via env var for temporary troubleshooting.
    introspect-fail-open: ${GATEWAY_AUTH_INTROSPECT_FAIL_OPEN:false}
